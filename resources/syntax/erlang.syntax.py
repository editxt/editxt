# -*- coding: UTF-8 -*-
# Syntax definition automatically generated by hljs2xt.py
# source: erlang.js
name = 'Erlang'
file_patterns = ['*.erlang', '*.erl']

literal = ['false', 'true']

keyword = [
    'after',
    'and',
    'andalso',
    'band',
    'begin',
    'bnot',
    'bor',
    'bsl',
    'bzr',
    'bxor',
    'case',
    'catch',
    'cond',
    'div',
    'end',
    'fun',
    'if',
    'let',
    'not',
    'of',
    'orelse',
    'query',
    'receive',
    'rem',
    'try',
    'when',
    'xor',
]

doctag = [RE(r"(?:TODO|FIXME|NOTE|BUG|XXX):")]

class comment:
    default_text = DELIMITER
    word_groups = [('doctag', doctag)]

class _group6:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group12:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

number = [RE(r"\b(\d+#[a-fA-F0-9]+|\d+(\.\d+)?([eE][-+]?\d+)?)")]

class _group18:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group29:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group26:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group29', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group29),
        ('_group30', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group32', RE(r"{"), [RE(r"}")]),
    ]

class _group24:
    default_text = DELIMITER
    delimited_ranges = [('_group26', RE(r"{"), [RE(r"}")], _group26)]

class _group15:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group18', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group18),
        ('_group19', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group21', RE(r"{"), [RE(r"}")]),
        ('_group24', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group24),
    ]

class _group43:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group49:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group46:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group49', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group49),
        ('_group50', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group52', RE(r"{"), [RE(r"}")]),
    ]

class _group40:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group43', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group43),
        ('_group44', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group46', RE(r"{"), [RE(r"}")], _group46),
    ]

class _group38:
    default_text = DELIMITER
    delimited_ranges = [('_group40', RE(r"{"), [RE(r"}")], _group40)]

class _group9:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group12', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group12),
        ('_group13', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group15', RE(r"{"), [RE(r"}")], _group15),
        ('_group38', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group38),
    ]

class _group7:
    default_text = DELIMITER
    delimited_ranges = [('_group9', RE(r"\("), [RE(r"(?=\))")], _group9)]

class _group63:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group69:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group80:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group77:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group80', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group80),
        ('_group81', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group83', RE(r"{"), [RE(r"}")]),
    ]

class _group75:
    default_text = DELIMITER
    delimited_ranges = [('_group77', RE(r"{"), [RE(r"}")], _group77)]

class _group66:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group69', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group69),
        ('_group70', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group72', RE(r"{"), [RE(r"}")]),
        ('_group75', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group75),
    ]

class _group64:
    default_text = DELIMITER
    delimited_ranges = [('_group66', RE(r"\("), [RE(r"(?=\))")], _group66)]

class _group96:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group102:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group99:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group102', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group102),
        ('_group103', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group105', RE(r"{"), [RE(r"}")]),
    ]

class _group97:
    default_text = DELIMITER
    delimited_ranges = [('_group99', RE(r"\("), [RE(r"(?=\))")], _group99)]

class _group93:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group96', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group96),
        ('_group97', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group97),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group110', RE(r"{"), [RE(r"}")]),
    ]

class _group91:
    default_text = DELIMITER
    delimited_ranges = [('_group93', RE(r"{"), [RE(r"}")], _group93)]

class _group60:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group63', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group63),
        ('_group64', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group64),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group88', RE(r"{"), [RE(r"}")]),
        ('_group91', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group91),
    ]

class _group121:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group127:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group133:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group130:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group133', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group133),
        ('_group134', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group136', RE(r"{"), [RE(r"}")]),
    ]

class _group124:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group127', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group127),
        ('_group128', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group130', RE(r"{"), [RE(r"}")], _group130),
    ]

class _group122:
    default_text = DELIMITER
    delimited_ranges = [('_group124', RE(r"\("), [RE(r"(?=\))")], _group124)]

class _group147:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group153:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group150:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group153', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group153),
        ('_group154', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group156', RE(r"{"), [RE(r"}")]),
    ]

class _group148:
    default_text = DELIMITER
    delimited_ranges = [('_group150', RE(r"\("), [RE(r"(?=\))")], _group150)]

class _group144:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group147', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group147),
        ('_group148', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group148),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group161', RE(r"{"), [RE(r"}")]),
    ]

class _group118:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group121', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group121),
        ('_group122', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group122),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group144', RE(r"{"), [RE(r"}")], _group144),
    ]

class _group116:
    default_text = DELIMITER
    delimited_ranges = [('_group118', RE(r"{"), [RE(r"}")], _group118)]

class _group2:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group6', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group6),
        ('_group7', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group7),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group60', RE(r"{"), [RE(r"}")], _group60),
        ('_group116', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group116),
    ]

class _group177:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group183:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group194:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group191:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group194', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group194),
        ('_group195', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group197', RE(r"{"), [RE(r"}")]),
    ]

class _group189:
    default_text = DELIMITER
    delimited_ranges = [('_group191', RE(r"{"), [RE(r"}")], _group191)]

class _group180:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group183', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group183),
        ('_group184', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group186', RE(r"{"), [RE(r"}")]),
        ('_group189', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group189),
    ]

class _group208:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group214:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group211:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group214', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group214),
        ('_group215', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group217', RE(r"{"), [RE(r"}")]),
    ]

class _group205:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group208', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group208),
        ('_group209', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group211', RE(r"{"), [RE(r"}")], _group211),
    ]

class _group203:
    default_text = DELIMITER
    delimited_ranges = [('_group205', RE(r"{"), [RE(r"}")], _group205)]

class _group173:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group177', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group177),
        ('_group178', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group180', RE(r"{"), [RE(r"}")], _group180),
        ('_group203', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group203),
    ]

class _group233:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group244:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group241:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group244', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group244),
        ('_group245', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group247', RE(r"{"), [RE(r"}")]),
    ]

class _group239:
    default_text = DELIMITER
    delimited_ranges = [('_group241', RE(r"{"), [RE(r"}")], _group241)]

class _group229:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group233', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group233),
        ('_group234', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group236', RE(r"{"), [RE(r"}")]),
        ('_group239', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group239),
    ]

class _group265:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group261:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group265', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group265),
        ('_group266', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group268', RE(r"{"), [RE(r"}")]),
    ]

class _group258:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group261', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group261),
        ('_group272', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group274', RE(r"{"), [RE(r"}")]),
    ]

class _group256:
    default_text = DELIMITER
    delimited_ranges = [('_group258', RE(r"{"), [RE(r"}")], _group258)]

class _group226:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group229', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group229),
        ('_group251', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group253', RE(r"{"), [RE(r"}")]),
        ('_group256', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group256),
    ]

class _group289:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group295:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group292:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group295', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group295),
        ('_group296', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group298', RE(r"{"), [RE(r"}")]),
    ]

class _group285:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group289', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group289),
        ('_group290', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group292', RE(r"{"), [RE(r"}")], _group292),
    ]

class _group314:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group310:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group314', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group314),
        ('_group315', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group317', RE(r"{"), [RE(r"}")]),
    ]

class _group307:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group310', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group310),
        ('_group321', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group323', RE(r"{"), [RE(r"}")]),
    ]

class _group282:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group285', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group285),
        ('_group305', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group307', RE(r"{"), [RE(r"}")], _group307),
    ]

class _group280:
    default_text = DELIMITER
    delimited_ranges = [('_group282', RE(r"{"), [RE(r"}")], _group282)]

class _group170:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group173', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group173),
        ('_group224', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group226', RE(r"{"), [RE(r"}")], _group226),
        ('_group280', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group280),
    ]

class _group168:
    default_text = DELIMITER
    delimited_ranges = [('_group170', RE(r"\("), [RE(r"(?=\))")], _group170)]

class _group338:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group344:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group355:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group352:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group355', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group355),
        ('_group356', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group358', RE(r"{"), [RE(r"}")]),
    ]

class _group350:
    default_text = DELIMITER
    delimited_ranges = [('_group352', RE(r"{"), [RE(r"}")], _group352)]

class _group341:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group344', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group344),
        ('_group345', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group347', RE(r"{"), [RE(r"}")]),
        ('_group350', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group350),
    ]

class _group339:
    default_text = DELIMITER
    delimited_ranges = [('_group341', RE(r"\("), [RE(r"(?=\))")], _group341)]

class _group371:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group377:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group374:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group377', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group377),
        ('_group378', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group380', RE(r"{"), [RE(r"}")]),
    ]

class _group372:
    default_text = DELIMITER
    delimited_ranges = [('_group374', RE(r"\("), [RE(r"(?=\))")], _group374)]

class _group368:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group371', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group371),
        ('_group372', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group372),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group385', RE(r"{"), [RE(r"}")]),
    ]

class _group366:
    default_text = DELIMITER
    delimited_ranges = [('_group368', RE(r"{"), [RE(r"}")], _group368)]

class _group334:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group338', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group338),
        ('_group339', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group339),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group363', RE(r"{"), [RE(r"}")]),
        ('_group366', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group366),
    ]

class _group398:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group409:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group406:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group409', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group409),
        ('_group410', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group412', RE(r"{"), [RE(r"}")]),
    ]

class _group404:
    default_text = DELIMITER
    delimited_ranges = [('_group406', RE(r"{"), [RE(r"}")], _group406)]

class _group394:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group398', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group398),
        ('_group399', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group401', RE(r"{"), [RE(r"}")]),
        ('_group404', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group404),
    ]

class _group430:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group426:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group430', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group430),
        ('_group431', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group433', RE(r"{"), [RE(r"}")]),
    ]

class _group423:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group426', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group426),
        ('_group437', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group439', RE(r"{"), [RE(r"}")]),
    ]

class _group421:
    default_text = DELIMITER
    delimited_ranges = [('_group423', RE(r"{"), [RE(r"}")], _group423)]

class _group391:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group394', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group394),
        ('_group416', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group418', RE(r"{"), [RE(r"}")]),
        ('_group421', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group421),
    ]

class _group389:
    default_text = DELIMITER
    delimited_ranges = [('_group391', RE(r"\("), [RE(r"(?=\))")], _group391)]

class _group456:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group462:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group459:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group462', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group462),
        ('_group463', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group465', RE(r"{"), [RE(r"}")]),
    ]

class _group457:
    default_text = DELIMITER
    delimited_ranges = [('_group459', RE(r"\("), [RE(r"(?=\))")], _group459)]

class _group452:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group456', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group456),
        ('_group457', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group457),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group470', RE(r"{"), [RE(r"}")]),
    ]

class _group483:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group479:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group483', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group483),
        ('_group484', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group486', RE(r"{"), [RE(r"}")]),
    ]

class _group476:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group479', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group479),
        ('_group490', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group492', RE(r"{"), [RE(r"}")]),
    ]

class _group474:
    default_text = DELIMITER
    delimited_ranges = [('_group476', RE(r"\("), [RE(r"(?=\))")], _group476)]

class _group449:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group452', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group452),
        ('_group474', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group474),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group497', RE(r"{"), [RE(r"}")]),
    ]

class _group447:
    default_text = DELIMITER
    delimited_ranges = [('_group449', RE(r"{"), [RE(r"}")], _group449)]

class _group331:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group334', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group334),
        ('_group389', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group389),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group444', RE(r"{"), [RE(r"}")]),
        ('_group447', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group447),
    ]

class _group512:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group518:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group524:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group521:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group524', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group524),
        ('_group525', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group527', RE(r"{"), [RE(r"}")]),
    ]

class _group515:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group518', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group518),
        ('_group519', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group521', RE(r"{"), [RE(r"}")], _group521),
    ]

class _group513:
    default_text = DELIMITER
    delimited_ranges = [('_group515', RE(r"\("), [RE(r"(?=\))")], _group515)]

class _group538:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group544:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group541:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group544', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group544),
        ('_group545', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group547', RE(r"{"), [RE(r"}")]),
    ]

class _group539:
    default_text = DELIMITER
    delimited_ranges = [('_group541', RE(r"\("), [RE(r"(?=\))")], _group541)]

class _group535:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group538', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group538),
        ('_group539', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group539),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group552', RE(r"{"), [RE(r"}")]),
    ]

class _group508:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group512', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group512),
        ('_group513', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group513),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group535', RE(r"{"), [RE(r"}")], _group535),
    ]

class _group568:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group574:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group571:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group574', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group574),
        ('_group575', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group577', RE(r"{"), [RE(r"}")]),
    ]

class _group564:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group568', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group568),
        ('_group569', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group571', RE(r"{"), [RE(r"}")], _group571),
    ]

class _group593:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group589:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group593', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group593),
        ('_group594', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group596', RE(r"{"), [RE(r"}")]),
    ]

class _group586:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group589', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group589),
        ('_group600', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group602', RE(r"{"), [RE(r"}")]),
    ]

class _group561:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group564', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group564),
        ('_group584', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group586', RE(r"{"), [RE(r"}")], _group586),
    ]

class _group559:
    default_text = DELIMITER
    delimited_ranges = [('_group561', RE(r"\("), [RE(r"(?=\))")], _group561)]

class _group617:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group623:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group620:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group623', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group623),
        ('_group624', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group626', RE(r"{"), [RE(r"}")]),
    ]

class _group618:
    default_text = DELIMITER
    delimited_ranges = [('_group620', RE(r"\("), [RE(r"(?=\))")], _group620)]

class _group613:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group617', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group617),
        ('_group618', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group618),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group631', RE(r"{"), [RE(r"}")]),
    ]

class _group644:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group640:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group644', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group644),
        ('_group645', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group647', RE(r"{"), [RE(r"}")]),
    ]

class _group637:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group640', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group640),
        ('_group651', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group653', RE(r"{"), [RE(r"}")]),
    ]

class _group635:
    default_text = DELIMITER
    delimited_ranges = [('_group637', RE(r"\("), [RE(r"(?=\))")], _group637)]

class _group610:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group613', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group613),
        ('_group635', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group635),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group658', RE(r"{"), [RE(r"}")]),
    ]

class _group505:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group508', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group508),
        ('_group559', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group559),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group610', RE(r"{"), [RE(r"}")], _group610),
    ]

class _group503:
    default_text = DELIMITER
    delimited_ranges = [('_group505', RE(r"{"), [RE(r"}")], _group505)]

class params:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2),
        ('_group168', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group168),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group331', RE(r"{"), [RE(r"}")], _group331),
        ('_group503', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group503),
    ]

title = [RE(r"[a-z'][a-zA-Z0-9_']*")]

class function:
    default_text = DELIMITER
    word_groups = [('title', title)]
    delimited_ranges = [('params', RE(r"\("), [RE(r"\)")], params)]

class function0:
    default_text = DELIMITER
    delimited_ranges = [
        ('function', RE(r"(?=^[a-z'][a-zA-Z0-9_']*\s*\()"), [RE(r"->")], function),
    ]
function0.__name__ = 'function'

class _group672:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group678:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group684:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group695:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group692:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group695', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group695),
        ('_group696', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group698', RE(r"{"), [RE(r"}")]),
    ]

class _group690:
    default_text = DELIMITER
    delimited_ranges = [('_group692', RE(r"{"), [RE(r"}")], _group692)]

class _group681:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group684', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group684),
        ('_group685', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group687', RE(r"{"), [RE(r"}")]),
        ('_group690', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group690),
    ]

class _group709:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group715:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group712:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group715', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group715),
        ('_group716', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group718', RE(r"{"), [RE(r"}")]),
    ]

class _group706:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group709', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group709),
        ('_group710', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group712', RE(r"{"), [RE(r"}")], _group712),
    ]

class _group704:
    default_text = DELIMITER
    delimited_ranges = [('_group706', RE(r"{"), [RE(r"}")], _group706)]

class _group675:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group678', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group678),
        ('_group679', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group681', RE(r"{"), [RE(r"}")], _group681),
        ('_group704', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group704),
    ]

class _group673:
    default_text = DELIMITER
    delimited_ranges = [('_group675', RE(r"\("), [RE(r"(?=\))")], _group675)]

class _group729:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group735:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group746:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group743:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group746', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group746),
        ('_group747', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group749', RE(r"{"), [RE(r"}")]),
    ]

class _group741:
    default_text = DELIMITER
    delimited_ranges = [('_group743', RE(r"{"), [RE(r"}")], _group743)]

class _group732:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group735', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group735),
        ('_group736', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group738', RE(r"{"), [RE(r"}")]),
        ('_group741', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group741),
    ]

class _group730:
    default_text = DELIMITER
    delimited_ranges = [('_group732', RE(r"\("), [RE(r"(?=\))")], _group732)]

class _group762:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group768:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group765:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group768', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group768),
        ('_group769', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group771', RE(r"{"), [RE(r"}")]),
    ]

class _group763:
    default_text = DELIMITER
    delimited_ranges = [('_group765', RE(r"\("), [RE(r"(?=\))")], _group765)]

class _group759:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group762', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group762),
        ('_group763', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group763),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group776', RE(r"{"), [RE(r"}")]),
    ]

class _group757:
    default_text = DELIMITER
    delimited_ranges = [('_group759', RE(r"{"), [RE(r"}")], _group759)]

class _group726:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group729', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group729),
        ('_group730', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group730),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group754', RE(r"{"), [RE(r"}")]),
        ('_group757', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group757),
    ]

class _group787:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group793:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group799:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group796:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group799', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group799),
        ('_group800', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group802', RE(r"{"), [RE(r"}")]),
    ]

class _group790:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group793', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group793),
        ('_group794', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group796', RE(r"{"), [RE(r"}")], _group796),
    ]

class _group788:
    default_text = DELIMITER
    delimited_ranges = [('_group790', RE(r"\("), [RE(r"(?=\))")], _group790)]

class _group813:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group819:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group816:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group819', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group819),
        ('_group820', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group822', RE(r"{"), [RE(r"}")]),
    ]

class _group814:
    default_text = DELIMITER
    delimited_ranges = [('_group816', RE(r"\("), [RE(r"(?=\))")], _group816)]

class _group810:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group813', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group813),
        ('_group814', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group814),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group827', RE(r"{"), [RE(r"}")]),
    ]

class _group784:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group787', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group787),
        ('_group788', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group788),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group810', RE(r"{"), [RE(r"}")], _group810),
    ]

class _group782:
    default_text = DELIMITER
    delimited_ranges = [('_group784', RE(r"{"), [RE(r"}")], _group784)]

class _group668:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group672', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group672),
        ('_group673', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group673),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group726', RE(r"{"), [RE(r"}")], _group726),
        ('_group782', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group782),
    ]

class _group843:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group849:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group860:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group857:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group860', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group860),
        ('_group861', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group863', RE(r"{"), [RE(r"}")]),
    ]

class _group855:
    default_text = DELIMITER
    delimited_ranges = [('_group857', RE(r"{"), [RE(r"}")], _group857)]

class _group846:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group849', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group849),
        ('_group850', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group852', RE(r"{"), [RE(r"}")]),
        ('_group855', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group855),
    ]

class _group874:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group880:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group877:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group880', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group880),
        ('_group881', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group883', RE(r"{"), [RE(r"}")]),
    ]

class _group871:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group874', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group874),
        ('_group875', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group877', RE(r"{"), [RE(r"}")], _group877),
    ]

class _group869:
    default_text = DELIMITER
    delimited_ranges = [('_group871', RE(r"{"), [RE(r"}")], _group871)]

class _group839:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group843', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group843),
        ('_group844', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group846', RE(r"{"), [RE(r"}")], _group846),
        ('_group869', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group869),
    ]

class _group899:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group910:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group907:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group910', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group910),
        ('_group911', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group913', RE(r"{"), [RE(r"}")]),
    ]

class _group905:
    default_text = DELIMITER
    delimited_ranges = [('_group907', RE(r"{"), [RE(r"}")], _group907)]

class _group895:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group899', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group899),
        ('_group900', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group902', RE(r"{"), [RE(r"}")]),
        ('_group905', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group905),
    ]

class _group931:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group927:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group931', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group931),
        ('_group932', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group934', RE(r"{"), [RE(r"}")]),
    ]

class _group924:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group927', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group927),
        ('_group938', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group940', RE(r"{"), [RE(r"}")]),
    ]

class _group922:
    default_text = DELIMITER
    delimited_ranges = [('_group924', RE(r"{"), [RE(r"}")], _group924)]

class _group892:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group895', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group895),
        ('_group917', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group919', RE(r"{"), [RE(r"}")]),
        ('_group922', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group922),
    ]

class _group955:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group961:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group958:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group961', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group961),
        ('_group962', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group964', RE(r"{"), [RE(r"}")]),
    ]

class _group951:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group955', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group955),
        ('_group956', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group958', RE(r"{"), [RE(r"}")], _group958),
    ]

class _group980:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group976:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group980', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group980),
        ('_group981', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group983', RE(r"{"), [RE(r"}")]),
    ]

class _group973:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group976', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group976),
        ('_group987', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group989', RE(r"{"), [RE(r"}")]),
    ]

class _group948:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group951', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group951),
        ('_group971', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group973', RE(r"{"), [RE(r"}")], _group973),
    ]

class _group946:
    default_text = DELIMITER
    delimited_ranges = [('_group948', RE(r"{"), [RE(r"}")], _group948)]

class _group836:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group839', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group839),
        ('_group890', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group892', RE(r"{"), [RE(r"}")], _group892),
        ('_group946', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group946),
    ]

class _group834:
    default_text = DELIMITER
    delimited_ranges = [('_group836', RE(r"\("), [RE(r"(?=\))")], _group836)]

class _group1004:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1010:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1021:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1018:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1021', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1021),
        ('_group1022', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1024', RE(r"{"), [RE(r"}")]),
    ]

class _group1016:
    default_text = DELIMITER
    delimited_ranges = [('_group1018', RE(r"{"), [RE(r"}")], _group1018)]

class _group1007:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1010', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1010),
        ('_group1011', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1013', RE(r"{"), [RE(r"}")]),
        ('_group1016', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1016),
    ]

class _group1005:
    default_text = DELIMITER
    delimited_ranges = [('_group1007', RE(r"\("), [RE(r"(?=\))")], _group1007)]

class _group1037:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1043:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1040:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1043', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1043),
        ('_group1044', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1046', RE(r"{"), [RE(r"}")]),
    ]

class _group1038:
    default_text = DELIMITER
    delimited_ranges = [('_group1040', RE(r"\("), [RE(r"(?=\))")], _group1040)]

class _group1034:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1037', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1037),
        ('_group1038', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1038),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1051', RE(r"{"), [RE(r"}")]),
    ]

class _group1032:
    default_text = DELIMITER
    delimited_ranges = [('_group1034', RE(r"{"), [RE(r"}")], _group1034)]

class _group1000:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1004', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1004),
        ('_group1005', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1005),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1029', RE(r"{"), [RE(r"}")]),
        ('_group1032', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1032),
    ]

class _group1064:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1075:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1072:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1075', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1075),
        ('_group1076', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1078', RE(r"{"), [RE(r"}")]),
    ]

class _group1070:
    default_text = DELIMITER
    delimited_ranges = [('_group1072', RE(r"{"), [RE(r"}")], _group1072)]

class _group1060:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1064', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1064),
        ('_group1065', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1067', RE(r"{"), [RE(r"}")]),
        ('_group1070', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1070),
    ]

class _group1096:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1092:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1096', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1096),
        ('_group1097', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1099', RE(r"{"), [RE(r"}")]),
    ]

class _group1089:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1092', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1092),
        ('_group1103', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1105', RE(r"{"), [RE(r"}")]),
    ]

class _group1087:
    default_text = DELIMITER
    delimited_ranges = [('_group1089', RE(r"{"), [RE(r"}")], _group1089)]

class _group1057:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1060', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1060),
        ('_group1082', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1084', RE(r"{"), [RE(r"}")]),
        ('_group1087', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1087),
    ]

class _group1055:
    default_text = DELIMITER
    delimited_ranges = [('_group1057', RE(r"\("), [RE(r"(?=\))")], _group1057)]

class _group1122:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1128:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1125:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1128', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1128),
        ('_group1129', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1131', RE(r"{"), [RE(r"}")]),
    ]

class _group1123:
    default_text = DELIMITER
    delimited_ranges = [('_group1125', RE(r"\("), [RE(r"(?=\))")], _group1125)]

class _group1118:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1122', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1122),
        ('_group1123', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1123),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1136', RE(r"{"), [RE(r"}")]),
    ]

class _group1149:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1145:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1149', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1149),
        ('_group1150', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1152', RE(r"{"), [RE(r"}")]),
    ]

class _group1142:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1145', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1145),
        ('_group1156', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1158', RE(r"{"), [RE(r"}")]),
    ]

class _group1140:
    default_text = DELIMITER
    delimited_ranges = [('_group1142', RE(r"\("), [RE(r"(?=\))")], _group1142)]

class _group1115:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1118', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1118),
        ('_group1140', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1140),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1163', RE(r"{"), [RE(r"}")]),
    ]

class _group1113:
    default_text = DELIMITER
    delimited_ranges = [('_group1115', RE(r"{"), [RE(r"}")], _group1115)]

class _group997:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1000', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1000),
        ('_group1055', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1055),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1110', RE(r"{"), [RE(r"}")]),
        ('_group1113', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1113),
    ]

class _group1178:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1184:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1190:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1187:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1190', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1190),
        ('_group1191', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1193', RE(r"{"), [RE(r"}")]),
    ]

class _group1181:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1184', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1184),
        ('_group1185', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1187', RE(r"{"), [RE(r"}")], _group1187),
    ]

class _group1179:
    default_text = DELIMITER
    delimited_ranges = [('_group1181', RE(r"\("), [RE(r"(?=\))")], _group1181)]

class _group1204:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1210:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1207:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1210', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1210),
        ('_group1211', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1213', RE(r"{"), [RE(r"}")]),
    ]

class _group1205:
    default_text = DELIMITER
    delimited_ranges = [('_group1207', RE(r"\("), [RE(r"(?=\))")], _group1207)]

class _group1201:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1204', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1204),
        ('_group1205', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1205),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1218', RE(r"{"), [RE(r"}")]),
    ]

class _group1174:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1178', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1178),
        ('_group1179', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1179),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1201', RE(r"{"), [RE(r"}")], _group1201),
    ]

class _group1234:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1240:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1237:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1240', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1240),
        ('_group1241', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1243', RE(r"{"), [RE(r"}")]),
    ]

class _group1230:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1234', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1234),
        ('_group1235', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1237', RE(r"{"), [RE(r"}")], _group1237),
    ]

class _group1259:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1255:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1259', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1259),
        ('_group1260', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1262', RE(r"{"), [RE(r"}")]),
    ]

class _group1252:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1255', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1255),
        ('_group1266', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1268', RE(r"{"), [RE(r"}")]),
    ]

class _group1227:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1230', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1230),
        ('_group1250', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1252', RE(r"{"), [RE(r"}")], _group1252),
    ]

class _group1225:
    default_text = DELIMITER
    delimited_ranges = [('_group1227', RE(r"\("), [RE(r"(?=\))")], _group1227)]

class _group1283:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1289:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1286:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1289', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1289),
        ('_group1290', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1292', RE(r"{"), [RE(r"}")]),
    ]

class _group1284:
    default_text = DELIMITER
    delimited_ranges = [('_group1286', RE(r"\("), [RE(r"(?=\))")], _group1286)]

class _group1279:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1283', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1283),
        ('_group1284', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1284),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1297', RE(r"{"), [RE(r"}")]),
    ]

class _group1310:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1306:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1310', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1310),
        ('_group1311', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1313', RE(r"{"), [RE(r"}")]),
    ]

class _group1303:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1306', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1306),
        ('_group1317', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1319', RE(r"{"), [RE(r"}")]),
    ]

class _group1301:
    default_text = DELIMITER
    delimited_ranges = [('_group1303', RE(r"\("), [RE(r"(?=\))")], _group1303)]

class _group1276:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1279', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1279),
        ('_group1301', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1301),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1324', RE(r"{"), [RE(r"}")]),
    ]

class _group1171:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1174', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1174),
        ('_group1225', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1225),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1276', RE(r"{"), [RE(r"}")], _group1276),
    ]

class _group1169:
    default_text = DELIMITER
    delimited_ranges = [('_group1171', RE(r"{"), [RE(r"}")], _group1171)]

class _group665:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group668', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group668),
        ('_group834', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group834),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group997', RE(r"{"), [RE(r"}")], _group997),
        ('_group1169', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1169),
    ]

keyword0 = [
    '-module',
    '-record',
    '-undef',
    '-export',
    '-ifdef',
    '-ifndef',
    '-author',
    '-copyright',
    '-doc',
    '-vsn',
    '-import',
    '-include',
    '-include_lib',
    '-compile',
    '-define',
    '-else',
    '-endif',
    '-file',
    '-behaviour',
    '-behavior',
    '-spec',
]

class _group1339:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1345:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1351:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1362:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1359:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1362', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1362),
        ('_group1363', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1365', RE(r"{"), [RE(r"}")]),
    ]

class _group1357:
    default_text = DELIMITER
    delimited_ranges = [('_group1359', RE(r"{"), [RE(r"}")], _group1359)]

class _group1348:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1351', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1351),
        ('_group1352', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1354', RE(r"{"), [RE(r"}")]),
        ('_group1357', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1357),
    ]

class _group1376:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1382:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1379:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1382', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1382),
        ('_group1383', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1385', RE(r"{"), [RE(r"}")]),
    ]

class _group1373:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1376', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1376),
        ('_group1377', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1379', RE(r"{"), [RE(r"}")], _group1379),
    ]

class _group1371:
    default_text = DELIMITER
    delimited_ranges = [('_group1373', RE(r"{"), [RE(r"}")], _group1373)]

class _group1342:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1345', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1345),
        ('_group1346', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1348', RE(r"{"), [RE(r"}")], _group1348),
        ('_group1371', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1371),
    ]

class _group1340:
    default_text = DELIMITER
    delimited_ranges = [('_group1342', RE(r"\("), [RE(r"(?=\))")], _group1342)]

class _group1396:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1402:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1413:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1410:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1413', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1413),
        ('_group1414', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1416', RE(r"{"), [RE(r"}")]),
    ]

class _group1408:
    default_text = DELIMITER
    delimited_ranges = [('_group1410', RE(r"{"), [RE(r"}")], _group1410)]

class _group1399:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1402', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1402),
        ('_group1403', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1405', RE(r"{"), [RE(r"}")]),
        ('_group1408', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1408),
    ]

class _group1397:
    default_text = DELIMITER
    delimited_ranges = [('_group1399', RE(r"\("), [RE(r"(?=\))")], _group1399)]

class _group1429:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1435:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1432:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1435', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1435),
        ('_group1436', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1438', RE(r"{"), [RE(r"}")]),
    ]

class _group1430:
    default_text = DELIMITER
    delimited_ranges = [('_group1432', RE(r"\("), [RE(r"(?=\))")], _group1432)]

class _group1426:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1429', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1429),
        ('_group1430', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1430),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1443', RE(r"{"), [RE(r"}")]),
    ]

class _group1424:
    default_text = DELIMITER
    delimited_ranges = [('_group1426', RE(r"{"), [RE(r"}")], _group1426)]

class _group1393:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1396', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1396),
        ('_group1397', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1397),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1421', RE(r"{"), [RE(r"}")]),
        ('_group1424', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1424),
    ]

class _group1454:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1460:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1466:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1463:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1466', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1466),
        ('_group1467', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1469', RE(r"{"), [RE(r"}")]),
    ]

class _group1457:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1460', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1460),
        ('_group1461', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1463', RE(r"{"), [RE(r"}")], _group1463),
    ]

class _group1455:
    default_text = DELIMITER
    delimited_ranges = [('_group1457', RE(r"\("), [RE(r"(?=\))")], _group1457)]

class _group1480:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1486:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1483:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1486', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1486),
        ('_group1487', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1489', RE(r"{"), [RE(r"}")]),
    ]

class _group1481:
    default_text = DELIMITER
    delimited_ranges = [('_group1483', RE(r"\("), [RE(r"(?=\))")], _group1483)]

class _group1477:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1480', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1480),
        ('_group1481', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1481),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1494', RE(r"{"), [RE(r"}")]),
    ]

class _group1451:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1454', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1454),
        ('_group1455', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1455),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1477', RE(r"{"), [RE(r"}")], _group1477),
    ]

class _group1449:
    default_text = DELIMITER
    delimited_ranges = [('_group1451', RE(r"{"), [RE(r"}")], _group1451)]

class _group1335:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1339', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1339),
        ('_group1340', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1340),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1393', RE(r"{"), [RE(r"}")], _group1393),
        ('_group1449', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1449),
    ]

class _group1510:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1516:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1527:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1524:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1527', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1527),
        ('_group1528', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1530', RE(r"{"), [RE(r"}")]),
    ]

class _group1522:
    default_text = DELIMITER
    delimited_ranges = [('_group1524', RE(r"{"), [RE(r"}")], _group1524)]

class _group1513:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1516', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1516),
        ('_group1517', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1519', RE(r"{"), [RE(r"}")]),
        ('_group1522', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1522),
    ]

class _group1541:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1547:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1544:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1547', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1547),
        ('_group1548', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1550', RE(r"{"), [RE(r"}")]),
    ]

class _group1538:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1541', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1541),
        ('_group1542', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1544', RE(r"{"), [RE(r"}")], _group1544),
    ]

class _group1536:
    default_text = DELIMITER
    delimited_ranges = [('_group1538', RE(r"{"), [RE(r"}")], _group1538)]

class _group1506:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1510', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1510),
        ('_group1511', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1513', RE(r"{"), [RE(r"}")], _group1513),
        ('_group1536', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1536),
    ]

class _group1566:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1577:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1574:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1577', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1577),
        ('_group1578', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1580', RE(r"{"), [RE(r"}")]),
    ]

class _group1572:
    default_text = DELIMITER
    delimited_ranges = [('_group1574', RE(r"{"), [RE(r"}")], _group1574)]

class _group1562:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1566', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1566),
        ('_group1567', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1569', RE(r"{"), [RE(r"}")]),
        ('_group1572', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1572),
    ]

class _group1598:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1594:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1598', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1598),
        ('_group1599', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1601', RE(r"{"), [RE(r"}")]),
    ]

class _group1591:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1594', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1594),
        ('_group1605', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1607', RE(r"{"), [RE(r"}")]),
    ]

class _group1589:
    default_text = DELIMITER
    delimited_ranges = [('_group1591', RE(r"{"), [RE(r"}")], _group1591)]

class _group1559:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1562', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1562),
        ('_group1584', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1586', RE(r"{"), [RE(r"}")]),
        ('_group1589', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1589),
    ]

class _group1622:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1628:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1625:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1628', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1628),
        ('_group1629', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1631', RE(r"{"), [RE(r"}")]),
    ]

class _group1618:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1622', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1622),
        ('_group1623', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1625', RE(r"{"), [RE(r"}")], _group1625),
    ]

class _group1647:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1643:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1647', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1647),
        ('_group1648', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1650', RE(r"{"), [RE(r"}")]),
    ]

class _group1640:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1643', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1643),
        ('_group1654', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1656', RE(r"{"), [RE(r"}")]),
    ]

class _group1615:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1618', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1618),
        ('_group1638', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1640', RE(r"{"), [RE(r"}")], _group1640),
    ]

class _group1613:
    default_text = DELIMITER
    delimited_ranges = [('_group1615', RE(r"{"), [RE(r"}")], _group1615)]

class _group1503:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1506', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1506),
        ('_group1557', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1559', RE(r"{"), [RE(r"}")], _group1559),
        ('_group1613', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1613),
    ]

class _group1501:
    default_text = DELIMITER
    delimited_ranges = [('_group1503', RE(r"\("), [RE(r"(?=\))")], _group1503)]

class _group1671:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1677:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1688:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1685:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1688', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1688),
        ('_group1689', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1691', RE(r"{"), [RE(r"}")]),
    ]

class _group1683:
    default_text = DELIMITER
    delimited_ranges = [('_group1685', RE(r"{"), [RE(r"}")], _group1685)]

class _group1674:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1677', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1677),
        ('_group1678', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1680', RE(r"{"), [RE(r"}")]),
        ('_group1683', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1683),
    ]

class _group1672:
    default_text = DELIMITER
    delimited_ranges = [('_group1674', RE(r"\("), [RE(r"(?=\))")], _group1674)]

class _group1704:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1710:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1707:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1710', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1710),
        ('_group1711', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1713', RE(r"{"), [RE(r"}")]),
    ]

class _group1705:
    default_text = DELIMITER
    delimited_ranges = [('_group1707', RE(r"\("), [RE(r"(?=\))")], _group1707)]

class _group1701:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1704', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1704),
        ('_group1705', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1705),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1718', RE(r"{"), [RE(r"}")]),
    ]

class _group1699:
    default_text = DELIMITER
    delimited_ranges = [('_group1701', RE(r"{"), [RE(r"}")], _group1701)]

class _group1667:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1671', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1671),
        ('_group1672', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1672),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1696', RE(r"{"), [RE(r"}")]),
        ('_group1699', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1699),
    ]

class _group1731:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1742:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1739:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1742', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1742),
        ('_group1743', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1745', RE(r"{"), [RE(r"}")]),
    ]

class _group1737:
    default_text = DELIMITER
    delimited_ranges = [('_group1739', RE(r"{"), [RE(r"}")], _group1739)]

class _group1727:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1731', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1731),
        ('_group1732', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1734', RE(r"{"), [RE(r"}")]),
        ('_group1737', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1737),
    ]

class _group1763:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1759:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1763', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1763),
        ('_group1764', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1766', RE(r"{"), [RE(r"}")]),
    ]

class _group1756:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1759', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1759),
        ('_group1770', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1772', RE(r"{"), [RE(r"}")]),
    ]

class _group1754:
    default_text = DELIMITER
    delimited_ranges = [('_group1756', RE(r"{"), [RE(r"}")], _group1756)]

class _group1724:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1727', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1727),
        ('_group1749', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1751', RE(r"{"), [RE(r"}")]),
        ('_group1754', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1754),
    ]

class _group1722:
    default_text = DELIMITER
    delimited_ranges = [('_group1724', RE(r"\("), [RE(r"(?=\))")], _group1724)]

class _group1789:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1795:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1792:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1795', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1795),
        ('_group1796', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1798', RE(r"{"), [RE(r"}")]),
    ]

class _group1790:
    default_text = DELIMITER
    delimited_ranges = [('_group1792', RE(r"\("), [RE(r"(?=\))")], _group1792)]

class _group1785:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1789', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1789),
        ('_group1790', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1790),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1803', RE(r"{"), [RE(r"}")]),
    ]

class _group1816:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1812:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1816', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1816),
        ('_group1817', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1819', RE(r"{"), [RE(r"}")]),
    ]

class _group1809:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1812', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1812),
        ('_group1823', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1825', RE(r"{"), [RE(r"}")]),
    ]

class _group1807:
    default_text = DELIMITER
    delimited_ranges = [('_group1809', RE(r"\("), [RE(r"(?=\))")], _group1809)]

class _group1782:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1785', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1785),
        ('_group1807', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1807),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1830', RE(r"{"), [RE(r"}")]),
    ]

class _group1780:
    default_text = DELIMITER
    delimited_ranges = [('_group1782', RE(r"{"), [RE(r"}")], _group1782)]

class _group1664:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1667', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1667),
        ('_group1722', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1722),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1777', RE(r"{"), [RE(r"}")]),
        ('_group1780', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1780),
    ]

class _group1845:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1851:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1857:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1854:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1857', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1857),
        ('_group1858', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1860', RE(r"{"), [RE(r"}")]),
    ]

class _group1848:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1851', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1851),
        ('_group1852', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1854', RE(r"{"), [RE(r"}")], _group1854),
    ]

class _group1846:
    default_text = DELIMITER
    delimited_ranges = [('_group1848', RE(r"\("), [RE(r"(?=\))")], _group1848)]

class _group1871:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1877:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1874:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1877', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1877),
        ('_group1878', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1880', RE(r"{"), [RE(r"}")]),
    ]

class _group1872:
    default_text = DELIMITER
    delimited_ranges = [('_group1874', RE(r"\("), [RE(r"(?=\))")], _group1874)]

class _group1868:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1871', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1871),
        ('_group1872', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1872),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1885', RE(r"{"), [RE(r"}")]),
    ]

class _group1841:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1845', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1845),
        ('_group1846', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1846),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1868', RE(r"{"), [RE(r"}")], _group1868),
    ]

class _group1901:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1907:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1904:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1907', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1907),
        ('_group1908', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1910', RE(r"{"), [RE(r"}")]),
    ]

class _group1897:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1901', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1901),
        ('_group1902', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1904', RE(r"{"), [RE(r"}")], _group1904),
    ]

class _group1926:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1922:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1926', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1926),
        ('_group1927', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1929', RE(r"{"), [RE(r"}")]),
    ]

class _group1919:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1922', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1922),
        ('_group1933', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1935', RE(r"{"), [RE(r"}")]),
    ]

class _group1894:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1897', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1897),
        ('_group1917', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1919', RE(r"{"), [RE(r"}")], _group1919),
    ]

class _group1892:
    default_text = DELIMITER
    delimited_ranges = [('_group1894', RE(r"\("), [RE(r"(?=\))")], _group1894)]

class _group1950:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1956:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1953:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1956', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1956),
        ('_group1957', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1959', RE(r"{"), [RE(r"}")]),
    ]

class _group1951:
    default_text = DELIMITER
    delimited_ranges = [('_group1953', RE(r"\("), [RE(r"(?=\))")], _group1953)]

class _group1946:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1950', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1950),
        ('_group1951', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1951),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1964', RE(r"{"), [RE(r"}")]),
    ]

class _group1977:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group1973:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group1977', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1977),
        ('_group1978', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1980', RE(r"{"), [RE(r"}")]),
    ]

class _group1970:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1973', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1973),
        ('_group1984', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1986', RE(r"{"), [RE(r"}")]),
    ]

class _group1968:
    default_text = DELIMITER
    delimited_ranges = [('_group1970', RE(r"\("), [RE(r"(?=\))")], _group1970)]

class _group1943:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1946', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1946),
        ('_group1968', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1968),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1991', RE(r"{"), [RE(r"}")]),
    ]

class _group1838:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1841', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1841),
        ('_group1892', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1892),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1943', RE(r"{"), [RE(r"}")], _group1943),
    ]

class _group1836:
    default_text = DELIMITER
    delimited_ranges = [('_group1838', RE(r"{"), [RE(r"}")], _group1838)]

class params0:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group1335', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group1335),
        ('_group1501', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group1501),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group1664', RE(r"{"), [RE(r"}")], _group1664),
        ('_group1836', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1836),
    ]
params0.__name__ = 'params'

class _group1332:
    default_text = DELIMITER
    word_groups = [('keyword', keyword0)]
    delimited_ranges = [('params', RE(r"\("), [RE(r"\)")], params0)]

class _group2008:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2014:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2020:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2017:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2020', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2020),
        ('_group2021', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2023', RE(r"{"), [RE(r"}")]),
    ]

class _group2011:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2014', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2014),
        ('_group2015', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2017', RE(r"{"), [RE(r"}")], _group2017),
    ]

class _group2009:
    default_text = DELIMITER
    delimited_ranges = [('_group2011', RE(r"\("), [RE(r"(?=\))")], _group2011)]

class _group2034:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2040:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2037:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2040', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2040),
        ('_group2041', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2043', RE(r"{"), [RE(r"}")]),
    ]

class _group2035:
    default_text = DELIMITER
    delimited_ranges = [('_group2037', RE(r"\("), [RE(r"(?=\))")], _group2037)]

class _group2031:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2034', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2034),
        ('_group2035', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group2035),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2048', RE(r"{"), [RE(r"}")]),
    ]

class _group2004:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group2008', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2008),
        ('_group2009', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group2009),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2031', RE(r"{"), [RE(r"}")], _group2031),
    ]

class _group2064:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2070:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2067:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2070', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2070),
        ('_group2071', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2073', RE(r"{"), [RE(r"}")]),
    ]

class _group2060:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group2064', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2064),
        ('_group2065', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2067', RE(r"{"), [RE(r"}")], _group2067),
    ]

class _group2089:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2085:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group2089', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2089),
        ('_group2090', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2092', RE(r"{"), [RE(r"}")]),
    ]

class _group2082:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2085', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2085),
        ('_group2096', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2098', RE(r"{"), [RE(r"}")]),
    ]

class _group2057:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2060', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2060),
        ('_group2080', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2082', RE(r"{"), [RE(r"}")], _group2082),
    ]

class _group2055:
    default_text = DELIMITER
    delimited_ranges = [('_group2057', RE(r"\("), [RE(r"(?=\))")], _group2057)]

class _group2113:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2119:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2116:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2119', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2119),
        ('_group2120', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2122', RE(r"{"), [RE(r"}")]),
    ]

class _group2114:
    default_text = DELIMITER
    delimited_ranges = [('_group2116', RE(r"\("), [RE(r"(?=\))")], _group2116)]

class _group2109:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group2113', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2113),
        ('_group2114', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group2114),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2127', RE(r"{"), [RE(r"}")]),
    ]

class _group2140:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2136:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group2140', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2140),
        ('_group2141', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2143', RE(r"{"), [RE(r"}")]),
    ]

class _group2133:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2136', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2136),
        ('_group2147', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2149', RE(r"{"), [RE(r"}")]),
    ]

class _group2131:
    default_text = DELIMITER
    delimited_ranges = [('_group2133', RE(r"\("), [RE(r"(?=\))")], _group2133)]

class _group2106:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2109', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2109),
        ('_group2131', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group2131),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2154', RE(r"{"), [RE(r"}")]),
    ]

class _group2001:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2004', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2004),
        ('_group2055', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group2055),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2106', RE(r"{"), [RE(r"}")], _group2106),
    ]

class _group1999:
    default_text = DELIMITER
    delimited_ranges = [('_group2001', RE(r"{"), [RE(r"}")], _group2001)]

class _group2170:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2176:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2187:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2184:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2187', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2187),
        ('_group2188', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2190', RE(r"{"), [RE(r"}")]),
    ]

class _group2182:
    default_text = DELIMITER
    delimited_ranges = [('_group2184', RE(r"{"), [RE(r"}")], _group2184)]

class _group2173:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2176', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2176),
        ('_group2177', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2179', RE(r"{"), [RE(r"}")]),
        ('_group2182', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group2182),
    ]

class _group2171:
    default_text = DELIMITER
    delimited_ranges = [('_group2173', RE(r"\("), [RE(r"(?=\))")], _group2173)]

class _group2203:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2209:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2206:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2209', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2209),
        ('_group2210', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2212', RE(r"{"), [RE(r"}")]),
    ]

class _group2204:
    default_text = DELIMITER
    delimited_ranges = [('_group2206', RE(r"\("), [RE(r"(?=\))")], _group2206)]

class _group2200:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2203', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2203),
        ('_group2204', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group2204),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2217', RE(r"{"), [RE(r"}")]),
    ]

class _group2198:
    default_text = DELIMITER
    delimited_ranges = [('_group2200', RE(r"{"), [RE(r"}")], _group2200)]

class _group2166:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group2170', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2170),
        ('_group2171', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group2171),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2195', RE(r"{"), [RE(r"}")]),
        ('_group2198', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group2198),
    ]

class _group2230:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2241:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2238:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2241', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2241),
        ('_group2242', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2244', RE(r"{"), [RE(r"}")]),
    ]

class _group2236:
    default_text = DELIMITER
    delimited_ranges = [('_group2238', RE(r"{"), [RE(r"}")], _group2238)]

class _group2226:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group2230', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2230),
        ('_group2231', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2233', RE(r"{"), [RE(r"}")]),
        ('_group2236', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group2236),
    ]

class _group2262:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2258:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group2262', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2262),
        ('_group2263', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2265', RE(r"{"), [RE(r"}")]),
    ]

class _group2255:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2258', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2258),
        ('_group2269', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2271', RE(r"{"), [RE(r"}")]),
    ]

class _group2253:
    default_text = DELIMITER
    delimited_ranges = [('_group2255', RE(r"{"), [RE(r"}")], _group2255)]

class _group2223:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2226', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2226),
        ('_group2248', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2250', RE(r"{"), [RE(r"}")]),
        ('_group2253', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group2253),
    ]

class _group2221:
    default_text = DELIMITER
    delimited_ranges = [('_group2223', RE(r"\("), [RE(r"(?=\))")], _group2223)]

class _group2288:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2294:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2291:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2294', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2294),
        ('_group2295', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2297', RE(r"{"), [RE(r"}")]),
    ]

class _group2289:
    default_text = DELIMITER
    delimited_ranges = [('_group2291', RE(r"\("), [RE(r"(?=\))")], _group2291)]

class _group2284:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group2288', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2288),
        ('_group2289', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group2289),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2302', RE(r"{"), [RE(r"}")]),
    ]

class _group2315:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword)]

class _group2311:
    default_text = DELIMITER
    word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('', RE(r"'"), [RE(r"'")]),
        ('_group2315', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2315),
        ('_group2316', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2318', RE(r"{"), [RE(r"}")]),
    ]

class _group2308:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2311', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2311),
        ('_group2322', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")]),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2324', RE(r"{"), [RE(r"}")]),
    ]

class _group2306:
    default_text = DELIMITER
    delimited_ranges = [('_group2308', RE(r"\("), [RE(r"(?=\))")], _group2308)]

class _group2281:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2284', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2284),
        ('_group2306', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group2306),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2329', RE(r"{"), [RE(r"}")]),
    ]

class _group2279:
    default_text = DELIMITER
    delimited_ranges = [('_group2281', RE(r"{"), [RE(r"}")], _group2281)]

class _group2163:
    default_text = DELIMITER
    word_groups = [('number', number)]
    delimited_ranges = [
        ('comment', RE(r"%"), [RE(r"$")], comment),
        ('_group2166', RE(r"\b(fun|receive|if|try|case)"), [RE(r"end")], _group2166),
        ('_group2221', RE(r"(?=([a-z'][a-zA-Z0-9_']*:[a-z'][a-zA-Z0-9_']*|[a-z'][a-zA-Z0-9_']*)\()"), [RE(r"\)")], _group2221),
        ('string', RE(r"\""), [RE(r"\"")]),
        ('_group2276', RE(r"{"), [RE(r"}")]),
        ('_group2279', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group2279),
    ]

word_groups = [('literal', literal), ('keyword', keyword), ('number', number)]

delimited_ranges = [
    ('function', function0, [RE(r"(?=\B|\b)")], _group665),
    ('comment', RE(r"%"), [RE(r"$")], comment),
    ('_group1332', RE(r"(?=^-)"), [RE(r"(?=\.)")], _group1332),
    ('string', RE(r"\""), [RE(r"\"")]),
    ('_group1999', RE(r"(?=#[a-zA-Z_]\w*)"), [RE(r"\B|\b")], _group1999),
    ('_group2163', RE(r"{"), [RE(r"}")], _group2163),
]
