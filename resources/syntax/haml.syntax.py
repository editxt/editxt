# -*- coding: UTF-8 -*-
# Syntax definition automatically generated by hljs2xt.py
# source: haml.js
name = 'Haml'
file_patterns = ['*.haml']

flags = re.IGNORECASE | re.MULTILINE

meta = [RE(r"^!!!( (5|1\.1|Strict|Frameset|Basic|Mobile|RDFa|XML\b.*))?$")]

doctag = [RE(r"(?:TODO|FIXME|NOTE|BUG|XXX):")]

class comment:
    default_text = DELIMITER
    word_groups = [('doctag', doctag)]

selector_tag = [RE(r"\w+")]

selector_id = [RE(r"#[\w-]+")]

selector_class = [RE(r"\.[\w-]+")]

attr = [RE(r":\w+")]

class _group3:
    default_text = DELIMITER
    word_groups = [('attr', attr)]
    delimited_ranges = [
        ('string', RE(r"'"), [RE(r"'")]),
        ('string', RE(r"\""), [RE(r"\"")]),
    ]

class _group2:
    default_text = DELIMITER
    delimited_ranges = [('_group3', RE(r"(?=:\w+\s*=>)"), [RE(r",\s+")], _group3)]

class _group8:
    default_text = DELIMITER
    word_groups = [('attr', selector_tag)]
    delimited_ranges = [
        ('string', RE(r"'"), [RE(r"'")]),
        ('string', RE(r"\""), [RE(r"\"")]),
    ]

class _group7:
    default_text = DELIMITER
    delimited_ranges = [('_group8', RE(r"(?=\w+\s*=)"), [RE(r"\s+")], _group8)]

class tag:
    default_text = DELIMITER
    word_groups = [
        ('selector-tag', selector_tag),
        ('selector-id', selector_id),
        ('selector-class', selector_class),
    ]
    delimited_ranges = [
        ('_group2', RE(r"{\s*"), [RE(r"\s*}")], _group2),
        ('_group7', RE(r"\(\s*"), [RE(r"(?=\s*\))")], _group7),
    ]

word_groups = [('meta', meta)]

delimited_ranges = [
    ('comment', RE(r"^\s*(!=#|=#|-#|/).*$"), [RE(r"\B|\b")], comment),
    ('tag', RE(r"^\s*%"), [RE(r"\B|\b")], tag),
]
